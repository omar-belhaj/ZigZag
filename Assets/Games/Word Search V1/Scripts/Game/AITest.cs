using System.Collections;
using System.Collections.Generic;
using System.Threading.Tasks; // Ensure this is included
using UnityEngine;

public class AITest : MonoBehaviour
{
    // Start is now an async Task method
    private async Task Start()
    {
/*
        // Await the word generation function
        string[] words = await WordGeneration.instance.StartWordGeneration("animaux", 3, 5, 8);
        if (words != null)
        {
            Debug.Log("Generated words: " + string.Join(", ", words));
        }

        // Await the image generation function
        string imageUrl = await WordGeneration.instance.StartImageGeneration("un chat mignon", 8);
        if (!string.IsNullOrEmpty(imageUrl))
        {
            Debug.Log("Image URL: " + imageUrl);
        }*/
    }
}
